import React from 'react'

interface Props {}

function SearchUserPage(props: Props) {
    const {} = props

    return (
        <div>
            user
        </div>
    )
}

export default SearchUserPage;
